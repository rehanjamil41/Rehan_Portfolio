import ProjectGrid from "../components/work/ProjectGrid";
import React from "react";

const Work = () => {
  return (
    <section
      className="relative z-10 flex w-full flex-col items-center justify-center h-screen bg-gradient-to-r from-[#1f2024] to-[#0E1016]"
      id="work"
    >
      <div className="flex items-center justify-center h-full">
        <div className="animate-pulse text-6xl font-bold text-gray-500">
          Work (Pending...)
        </div>
      </div>
    </section>
  );
};

export default Work;
